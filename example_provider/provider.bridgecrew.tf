provider "bridgecrew" {
  # don't add your secrets to files that get put into git
  # this is isn't a valid api token so don't try using it.
  #url   = ""
  token = "ee60a054-b8f9-4b44-bc4e-e5f49d5bca5c"
}
